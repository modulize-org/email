import type { Properties } from 'csstype';

export const theme = {
  colors: <Properties>{
    color: '#000',
    backgroundColor: '#fff',
  },
  spacing: <Properties>{
    padding: '10px',
    margin: '10px',
  },
};