{
  "name": "@modulize/templates",
  "version": "0.0.18",
  "private": true,
  "main": "index.js",
  "workspaces": [
    ".react-email"
  ],
  "scripts": {
    "lint": "eslint \"**/*.ts*\"",
    "clean:dist": "rm -rf generated",
    "clean:dev": "rm -rf .react-email",
    "dev": "email dev --dir templates --port 4000",
    "export": "email export --dir templates --outDir generated",
    "build": "tsc"
  },
  "devDependencies": {
    "@modulize/eslint-config": "workspace:*",
    "@modulize/tsconfig": "workspace:*",
    "@types/node": "^20.4.10",
    "@types/react": "^18.2.20",
    "csstype": "^3.1.2",
    "react": "^17.0.2",
    "tsup": "^7.2.0",
    "tw-to-css": "^0.0.11",
    "typescript": "^4.5.2"
  },
  "dependencies": {
    "@react-email/components": "0.0.6",
    "react-email": "1.9.3"
  }
}